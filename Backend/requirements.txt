fastapi
uvicorn[standard]
gunicorn
motor
python-dotenv
certifi
pydantic
mongoengine
requests
httpx
anthropic
# For the FCM-V1-direct workaround (see app/core/fcm_direct.py).
# Remove once Expo's push-service bug is fixed and we revert to Expo-only.
google-auth
